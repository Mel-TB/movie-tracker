import styled from "styled-components";

const MainStyle = styled.main`
  display: flex;
  margin-top: 20px;
  height: calc(100vh - 73px - 3 * 27px);
  gap: 28px;
  justify-content: center;
`;

export default MainStyle;
